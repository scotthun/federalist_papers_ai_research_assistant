services:
  spike-db:
    image: pgvector/pgvector:pg18
    environment:
      POSTGRES_USER: spike
      POSTGRES_PASSWORD: spike
      POSTGRES_DB: spike
    ports:
      - "55432:5432"
    volumes:
      - spike-db-data:/var/lib/postgresql

volumes:
  spike-db-data:
